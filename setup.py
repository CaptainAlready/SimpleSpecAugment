from setuptools import setup, find_packages

setup(
    name='SimpleSpecAugment',
    version='0.0.1',
    packages=['SimpleSpecAugment'],
    install_requires=[
        'numpy',
    ],
    python_requires=">=3.7"
)
